import { StyleSheet } from "react-native";

export default StyleSheet.create({
    square: {
      width: 40,
      height: 40,
      margin: 1,
      justifyContent: 'center',
      alignItems: 'center',
    },
    text: {
      color: 'white',
      fontSize: 20,
    },
  });

